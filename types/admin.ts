//Admin types

export interface Admin {
    id: string;
    name: string;
    email: string;
    password: string;
    image?: string;
    role: string;
    createdAt: string;
    updatedAt: string;
}
